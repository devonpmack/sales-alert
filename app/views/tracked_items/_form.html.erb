<%= form_with(model: tracked_item, local: true) do |form| %>
  <% if tracked_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tracked_item.errors.count, "error") %> prohibited this tracked_item from being saved:</h2>

      <ul>
        <% tracked_item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :threshold %>
    <%= form.text_field :threshold %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
