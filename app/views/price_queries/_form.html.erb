<%= form_with(model: price_query, local: true) do |form| %>
  <% if price_query.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(price_query.errors.count, "error") %> prohibited this price_query from being saved:</h2>

      <ul>
        <% price_query.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
